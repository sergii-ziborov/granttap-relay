{
  "name": "granttap-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-knowledge WebSocket relay for GrantTap on Cloudflare Workers and Durable Objects.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/worker.js && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
